/**
 * Table formatter — thin re-export from ui/table.ts.
 * All 48 command files import from here. The actual renderer lives in ui/table.ts.
 */
export { formatTable, type TableColumn } from './ui/table.js';
