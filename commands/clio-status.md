---
description: "Every organization: open closes, deadlines, pending reviews"
argument-hint: ""
---

# Status across all organizations

Follow the **status** flow in the `clio-kit` skill (`references/flows.md`). Deadlines are computed from the filing fields the user recorded in ORG.md — see `references/workspace.md`.

## Usage

```
/clio-status
```
