---
description: "Connect, check, or rotate an organization's API key"
argument-hint: "[check | add | rotate]"
---

# Manage keys

Follow the **keys** flow in the `jaz-kit` skill (`references/flows.md`). The credential model is in `references/workspace.md`.

## Usage

```
/jk-keys
/jk-keys rotate
```
